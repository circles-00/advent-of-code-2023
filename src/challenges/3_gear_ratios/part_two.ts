import { join } from 'path'
import { readInput } from '../../utils'

const $3_gear_ratios_part_two = () => {
  const input = readInput(join(__dirname, './input.txt'))
  const inputLines = input.split('\n')
}

export default $3_gear_ratios_part_two
